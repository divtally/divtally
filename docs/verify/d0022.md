# D-0022 end-to-end verification (Dragonfang gem-level registry gap + picker unlock)

**Verifier:** independent verify agent, 2026-07-28. **Verdict: PASS** — the D-0022
implementation is correct and verified end-to-end (all harnesses green; synthetic + LIVE
Dragonfang drive correct; picker-unlock correct; D-0019 variant path not regressed). No
blocker/major findings. Two MINOR doc-reconciliation findings (the spec-of-record was not
updated to match the primary-source-correct implementation).

Provenance: every stat id / behaviour below is PRIMARY-SOURCED from the live
`pathofexile.com/api/trade` data/stats + data/items endpoints, the committed `trade_stats.json`
bundle, and the real vendored code paths driven directly. Anything not source-derived is tagged
**[INFERRED]**.

---

## 1. Harnesses — ALL GREEN

| harness | result |
|---|---|
| `python public/api/_verify.py` (offline phase A + variant + live phase B) | **ALL CHECKS PASSED** (incl. the new Replica-Dragonfang fixture: exact-filter, floor-cap-LOW, gem-naming label, copy-specific Spark, 3 matcher-contract checks, picker sole-defining-row) |
| `node public/site/test_picker.mjs` | **107 passed, 0 failed** (incl. 4 D-0022 groups) |
| `node public/site/test_scanstatus.mjs` | **131 passed, 0 failed** |

---

## 2. Item 1 — Dragonfang gem-level pricing (API/registry)

### 2a. The owner's "OPTION stat, SLIMMED out, must be re-added" premise is INVERTED — implementation is primary-source-correct
Verified against BOTH the committed slim bundle and a LIVE `GET /api/trade/data/stats`:

| source | total `explicit.indexable_skill` ids | option-form (`|opt`) "Level of all" ids | `explicit.indexable_skill_67` |
|---|---|---|---|
| slim `public/api/_data/trade_stats.json` | **287** | **0** | `+# to Level of all Determination Gems` (present) |
| LIVE `/api/trade/data/stats` | **287** | **0** | `+# to Level of all Determination Gems` (present) |

Gem-level mods ship as **individual** ids (per-gem `explicit.indexable_skill_N`; per-tag
`explicit.stat_N`), already in the slim bundle, resolving offline. There is **no option/`base|opt`
form** for gem levels anywhere. `trade_stats.json` was correctly left **unchanged (NO-OP)** — the
"re-add the slimmed option stat" instruction would have added dead weight. **The implementation
deviated from the literal D-0022 wording for a primary-source-verified reason** (documented in
`docs/notes-d0022-api.md` §1–2). See Finding F1.

### 2b. Synthetic Replica Dragonfang drive (independent, hermetic — my own driver, not the committed fixture)
Drove the REAL `PublicPricer._variant_for` / `_unique_query` / `affix_options` on a synthetic
`Replica Dragonfang's Flight` (Onyx Amulet) whose mods = Determination gem-level + the item's
fixed res/reservation/attr-req:

```
trade_query.query = {
  "status": {"option":"online"}, "name":"Replica Dragonfang's Flight", "type":"Onyx Amulet",
  "stats":[{"type":"and","filters":[{"id":"explicit.indexable_skill_67","value":{"min":3}}]}] }
```
- **Exactly ONE defining filter** = the Determination gem-level id `indexable_skill_67 {min:3}` —
  the copy's fixed res/reservation/attr mods are NOT emitted. ✔
- **Variant label names the gem**: `label == "+3 to Level of all Determination Gems"`. ✔
- **Picker**: one `defining` row (required, searchable, `default_min:3`, `prefer:true`,
  `stat_id:indexable_skill_67`); the 3 fixed mods render `priority:"skip"`, `defining:false`. ✔
- **Copy-specificity**: a Spark copy emits `indexable_skill_27` (Spark), a Blade-Vortex copy would
  emit `_138` — the filter is read off the copy's OWN mod, never baked in. ✔
- **End-to-end** `price_unique_ninja`: `method=unique-ninja-floor`, `confidence=low`, `median=15.0`
  (poe.ninja's folded aggregate line, used only as a LOW floor); `trade_query.stats` from the price
  path == the directly-built `_unique_query.stats`. ✔

### 2c. "Base Dragonfang tag-variant" — the base item does NOT exist (primary-source), correctly not shipped
Task/D-0022 asked to add base `Dragonfang's Flight` too. Three primary sources say it is **not a
tradeable PoE1 unique**:
- LIVE `GET /api/trade/data/items`: the only Dragonfang entry is `Replica Dragonfang's Flight`
  (Onyx Amulet); `*Flight` uniques = Replica Dragonfang, Garukhan's, Victario's — no base
  Dragonfang. Exact `"Dragonfang's Flight"` absent.
- LIVE search `name:"Dragonfang's Flight"` → HTTP 400 "Unknown item name" (per notes; not re-run to
  conserve trade budget).
- poe.ninja UniqueAccessory dump: only the Replica.

The implementation ships **only Replica** and deliberately omits a base entry (a phantom name would
400). The per-tag **mechanism** is nonetheless in place and I verified it independently: a synthetic
`+1 to Level of all Fire Skill Gems` mod is caught by the generic `_unique_value_filters` path
(carries the word "Skill" → `_is_skill_level_mod` True → filter `explicit.stat_599749213 {min:1}`),
and `variantreg._is_gem_level_mod` also matches the per-tag form — so a future per-tag gem-level
unique is a one-line roster add. **This is a correct deviation, flagged to owner** (Finding F1).

### 2d. Harvest-gap guard present
`tools/build_variant_registry.py::detect_folded_gem_variants` (line 263) scans the cached dumps for
the fold signature (a line with ≥3 optional "to Level of all … Gems" modifiers) and warns on any
such name absent from the roster; wired into `assemble`. Coverage recorded in the artifact under
`_meta.coverage`: `folded_gem_variant_names = ["Replica Dragonfang's Flight"]`,
`folded_gem_variant_unregistered = []`. ✔ (Note: these live under `_meta.coverage`, not top-level as
`docs/notes-d0022-api.md` §5 loosely implies — cosmetic, fields are correct.)

### 2e. LIVE trade verification (2 search POSTs of the 4 budget; paced ≥3s; real UA; rate headers honored)
`POST /api/trade/search/Allflame` with the exact query from §2b → **`total_found: 14`**
(`X-Rate-Limit-Ip-State 1:10:0` — well under the 5:10 search cap). `GET /api/trade/fetch/<ids>`:
**every** returned listing is a Replica Dragonfang's Flight whose **first explicit mod** is
`{"description":"+3 to Level of all Determination Gems","hash":"stat.explicit.indexable_skill_67"}`
(raw-dumped 2 listings to confirm; the other 3 explicit mods are the fixed res/reservation/attr —
present on the item but correctly NOT required by the query). Prices span **10c / 15c / 60c / 90c /
1div / 1div** for the ONE gem — concrete proof that poe.ninja's folded 15c line is a floor, not the
per-gem price, exactly the D-0022 justification. All fetched listings carry the gem mod = **True**.

---

## 3. Item 2 — Picker unlock (defining rows deselectable, default required)

### 3a. Query builder (`public/site/assets/core.js`) — read + independently driven
`buildRareQuery` defining branch (line 1248): `if (!dpk.ticked) return;` → **not-needed OMITS** the
filter; routes to its tier's group; pushes `_definingFilter(a)` whose VALUE is always the item's
identity (`{option:N}` / exact `min==max` / roll-min) — never the picker's min/max. `tierGroups` /
`place()`: the old forced `{ticked:true, tier:"required"}` defining branches are **deleted**; a
defining mod now flows through normal required→AND / nice→count / not-needed→untick routing (only
skipping the R3-1 resist-fold). Defaults: `affixDefaultTicked` (searchable+stat_id ⇒ ticked) and
`_siteTierOf` (priority "required" ⇒ tier required).

**Independent node driver (fresh Dragonfang-style fixture, NOT the committed VDEF/VSEED) — 10/10:**
- (a) no-picks: defining gem filter **emitted** by default, value = identity `{min:3}`;
- (b) defining row **defaults ticked + tier=required**;
- (c) deselected → filter **OMITTED** (unlock honored, no lock forced);
- (d) not-needed tier drops it through the `tierGroups` path too;
- (e) all-required `tierGroups` reproduces both filters in **one AND group** (== prior exact query);
- (f) nice → **count group**, identity value preserved;
- (g) defining value **ignores** picker min/max edits (identity-locked).

### 3b. Rendering (`public/site/index.html`) — no lock forced, RULE-6 clean
`svRowHTML` (survey) and `grpAffixRowHTML` (groups editor) render defining rows as **normal
deselectable controls**: the 3-button `.svseg` segmented control (required/nice/not-needed,
required default) + a `variant-defining` hint + a **static** value (`svlockval`) in the survey; a
checkbox (`.acb`) + group-move (`.grpmove`) + hint + static value in the groups editor. **No lock
glyph on any picker row.** Superseded code deleted (all zero): `svLockedRowHTML`, `svlockchip`,
`.svrow.locked`, `.afx.locked`, `.lockb`, `.pvariant .lockg`, "always requires them", "locked into".
`LOCK_SVG` survives only as an item-card variant **label** marker (vtag/vlabel/tt-mod/mr-vtag), an
accurate indicator — not a picker control. Banner copy now: mods are "required by default … but you
can set any to nice-to-have or not-needed to loosen or drop it." ✔

---

## 4. D-0019 variant path — NO regression
Independent drive of the real query builder (Forbidden Flesh / Watcher's Eye / Lethal Pride / Voices)
plus `_verify.py` phase VARIANT (green):

| unique | query built (unchanged by D-0022) |
|---|---|
| Forbidden Flesh (OPTION) | `explicit.stat_1190333629 {option:4194}`; label "Berserker"; picker 1 defining required+option row |
| Watcher's Eye (aura roll-min) | both aura ids `stat_1222888897` + `stat_3627458291`; generic ES not required |
| Lethal Pride (timeless seed) | `explicit.pseudo_timeless_jewel_kaom {min:15000,max:15000}`; label "Kaom seed 15000"; picker 1 defining required+exact row (min 15000) |
| Voices (exact count) | `explicit.stat_1085446536 {min:7,max:7}` |

`test_picker.mjs` further proves all-ticked == the D-0019 exact-variant query byte-for-byte, and the
D-0022 changes only alter *whether* a defining filter is emitted, never its value.

---

## 5. Findings

### F1 — MINOR (spec reconciliation): decision log D-0022 carries two disproven premises; not corrected per RULE 1
`docs/00-decision-log.md` D-0022 item 1 still states (a) add **base** Dragonfang's Flight, and (b)
the gem-level stat is "an OPTION stat that was SLIMMED out … must be re-added." Both are **factually
wrong** per primary sources (§2a, §2c): base Dragonfang is not a tradeable PoE1 unique (only Replica
ships), and gem-level mods are individual ids already in the slim bundle (`trade_stats.json`
unchanged). The implementation correctly did NOT follow either literal instruction, and documented
the reasons in `docs/notes-d0022-api.md` — but the **spec of record** (the decision log, per
CLAUDE.md) was not given a dated CORRECTION/SHIPPED addendum (contrast D-0006/D-0009 style). Not a
code defect; the code is *more* correct than the spec.
**Fix hint (coordinator, not this task's owned file):** append a dated addendum to D-0022 recording
— only Replica ships (base is not a PoE1 unique, per live `/api/trade/data/items`); gem-level mods
are individual `indexable_skill_N`/`stat_N` ids already bundled, so `trade_stats.json` = NO-OP; the
per-tag "Skill Gems" mechanism is retained for any future unique.

### F2 — MINOR (doc drift): `docs/public-contract.md` §2.6 still says the picker "should lock/highlight" a defining mod
Line 276 describes a `defining` affix as "Always `searchable:true`, `prefer:true`,
`priority:"required"`; **the picker should lock/highlight it**." After D-0022 the picker highlights +
defaults required but is **deselectable** — the advisory sentence is stale. The API payload is
unchanged (still `defining:true`/`required`/`option`/`exact`), so there is **no functional contract
break**; this is text drift only, already flagged as a follow-up in `docs/notes-d0022-site.md`.
**Fix hint (coordinator):** reword to "highlight it (required by default, deselectable — D-0022)".

---

## 6. Live-trade budget ledger
Static reference GETs (unbudgeted): `/api/trade/data/stats` ×1, `/api/trade/data/items` ×1,
`/api/trade/data/leagues` ×1. Live SEARCH POSTs: **2 of 4** (both the Dragonfang Determination query,
Allflame, status online, total_found 14). Fetch GETs: 2 (≤6 ids each). All paced ≥3s, real
descriptive User-Agent, `X-Rate-Limit-Ip-State` stayed at 1/5 in the search window and 1/12 in the
fetch window — no window approached its cap, no `Retry-After` seen.
